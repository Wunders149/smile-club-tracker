## Packages
date-fns | Date formatting and manipulation for events
framer-motion | Page transitions and smooth UI micro-interactions
@hookform/resolvers | Form validation with Zod
react-hook-form | Form state management

## Notes
Assuming POSITIONS and EVENT_TYPES are exported from @shared/schema
Assuming Date strings from the API need manual parsing when using z.custom in the shared schema
